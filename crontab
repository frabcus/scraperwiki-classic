# m h  dom mon dow   command

# Run scrapers ar 44 minutes passed midnight every day
# Note: This can take a very long time, so do anything that needs to be run *after* the scrapres have
# run no sooner than 6 hours after this (7AM)
44 0 * * * cd /var/www/alpha.scraperwiki.com/ && source bin/activate && cd web && python manage.py run_scrapers

# Collate email alerts every 5 minutes
# Note: this only queues them, see below for sending
*/5 * * * * cd /var/www/alpha.scraperwiki.com/ && source bin/activate && cd web && python manage.py email_alerts

# Update various bits of meta information for each scraper every hour
0 * * * * cd /var/www/alpha.scraperwiki.com/ && source bin/activate && cd web && python manage.py update_scraper_meta

# Send emails with django mailer every minute
#* * * * * cd /var/www/alpha.scraperwiki.com/ && source bin/activate && cd web && python manage.py email_alerts
