DEPLOYING

Requirements
============

fabric (fabfile.org). this should have been installed by the buildout (see INSTALL.txt)


Deploying to a server
=====================

In the root directory of your local install of ScraperWiki run

> $ fab [dev|live] webserver:buildout=no

